Выводы

В результате анализа EDA влияние параметров на успеваемость были получены следующие выводы:

1) из 30-ти солбцов датасета 3 вне имеют пропусков, 
   13 столбцов с числовыми значениями, пригодными для корреляционного анализа
   17 столбцов типа object для номинативного анализа.
2) Корреляционный анализ позволил выделить несколько столбцов, влияющих на score
 - Age (обратная зависимость), Medu (прямая зависимость), studytime (прямая завиисимость), 
   failures (обратная зависимость), styditime granular( обратная зависимость), goout(обратная зависимость)
3) номинативный  гарфический анализ факторов, влияющих на score выделил: 
   address, Mjob,Fjob, famsize, Pstatus, reason, guardian, famsup, paid, 
   nursery, activity, schoolsup, higher, internet,romantic,
   но стаически занчимый столбец только один  - Mjob ( 5 уникальных значений).
4) для построения модели предлагается использовать параметры:
   'Mjob', 'age', 'Medu', 'studytime', 'failures', 'studytime, granular', 'goout'
